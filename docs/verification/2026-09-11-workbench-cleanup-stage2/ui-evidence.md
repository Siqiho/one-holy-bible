# 第二批真实页面验收

日期：2026-09-11。Codex 内置浏览器，`http://127.0.0.1:1420/`，标题 One Holy Bible。同步线启动 PID 53819，cwd `/Users/simon/OHB/one-holy-bible`；本线未启动或重启共享服务。当前 TSX 与 CSS 的服务返回内容均已与磁盘逐字核对，见 runtime-identity.json。

## 经文搜索

- 搜“光”并 Enter：总数 429，首批结果按钮 120，显示“已显示 120 / 共 429 处结果”。
- 通过“加载更多结果”键盘入口逐次到 240、360、429；最后入口消失，未一次渲染全部结果。
- 第 121 条 `Ps.38.10` 实际跳转到诗篇 38 章，结果面板关闭。随后返回创世记 1 章。
- 加载“光”到 240 后改草稿为“神”：显示“待搜索”，加载更多隐藏；提交后回到 120 / 4029。
- “神”加载到 240，再切“新约”：回到 120 / 1307；再加载到 240 并切“和合本”，仍重新从 120 / 1307 开始。
- 初轮 UI 发现分页已复位而右下角仍显示旧“已显示 240”计数，已追加修复和回归断言。最终刷新页面后重复上述筛选，右下角变为“搜索到 1307 处经文”，不再保留旧 240 提示。

## 灯箱与相邻导航

- 从中间当前经文卡片列表的“放大希伯来文创世记残片”按钮按 Enter 打开。
- 焦点主动进入“关闭图片预览”；点击图片本身不关闭；Tab 和 Shift+Tab 都保留在关闭按钮。
- Esc 后 dialog 数量为 0，焦点回到原“放大希伯来文创世记残片”按钮。
- 重新打开，用关闭按钮关闭，焦点正确返回。再次打开，点击截图中无遮挡背景 x=40,y=350，灯箱关闭且焦点正确返回。
- 第一批书卷／章节弹层复测：分别从弹层内部选项按 Esc，均关闭并返回对应触发按钮。

## 可见反馈及残留

- 现有搜索反馈真实矩形约 186×36 px，position=fixed、visibility=visible、role=status、aria-live=polite；不再是 1×1 sr-only 节点。
- 点击现有“保存布局”：可见“布局已保存”，约 90×36 px，aria-live 保留；没有删除按钮或添加隐式回调。
- 右侧“起初，神创造天地”卡片复制标题：出现“已复制：标题”，剪贴板读回与标题一致。测试后清空该测试复制内容（原读回为空）。剪贴板失败与保留重试菜单路径由自动测试覆盖，未人为破坏真实浏览器权限。
- 本次浏览器警告／错误日志为空。最终筛选恢复全部译本／整本、搜索清空、弹层关闭，恢复 Reader 创世记 1 章。
- 没有执行资源编辑、删除、退出同步、同步、刷新卡片或重置布局；保存按钮仅验证其既有布局保存行为。

## 补查相关浮层

- 复制菜单：从右侧卡片入口 Enter 打开，焦点为“复制标题”menuitem；Esc 关闭，焦点回到该卡片的复制入口。再次打开后点左侧“资源库”非交互区域，菜单关闭并返回入口。自动测试还确认内部点击不关闭、复制失败保留重试。
- 搜索浮层：加载“光”到 240 条后，从加载更多按钮按 Esc，面板关闭、输入仍为“光”、焦点回“搜索经文”，没有被 input onFocus 意外重新打开。重新提交后点击内部译本筛选，面板保持；点外部“资源库”，面板关闭但查询保留。
- 设置：打开“阅读台设置”，从“关闭纸张模式”按钮按 Esc（没有切换主题），设置关闭且焦点回“打开设置”。
- 完成相关浮层修改后，再测灯箱 Esc 和章节弹层 Esc，关闭与焦点返回仍正常；日志仍无 warn/error。最终恢复全部译本、清空搜索，Reader 创世记 1 章。

后续修订包括搜索反馈 effect，以及复制菜单／搜索浮层／设置入口焦点；均有独立增量复核和回归测试记录。未修改卡片数据或主题值。

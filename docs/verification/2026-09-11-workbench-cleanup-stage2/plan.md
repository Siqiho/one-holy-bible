# Workbench cleanup stage 2 implementation plan

Goal: address DF-07 pagination, DF-01 image lightbox keyboard/focus, TB-15 visible existing feedback, without changing data or persistence semantics.

Execution: writing-plans / executing-plans workflow adapted to the user's direct continuation instruction. Codex implements because original Cursor job ae7ec5a5 remains uncertain. No old lock edits, replay, commits, deployment, dependencies or shared-service restart. Seek fresh independent read-only Cursor review of an isolated copy. All product edits limited to Workbench.tsx, Workbench.test.tsx, styles.css; this directory holds evidence. Back up all three originals and compare against those files, not dirty git HEAD.

Current evidence: searchResultsForDisplay fixes maxResults at 120 and renders only the truncated count; image preview has close-button/backdrop but no keyboard close/focus management; general status remains sr-only. All three still require implementation. Stage 1 changes are present and must remain.

- [x] DF-07: add local display-limit state starting at 120; pass it to searchResultsForDisplay(maxResults = searchResultLimit), retain search index totalCount. Render “已显示 X / 共 Y 处结果” plus “加载更多结果” while X < Y. Each click requests another 120, bounded by totalCount, and announces visible count. Reset on draft/submission, version, scope and current-book change. Existing filters and result-selection handlers remain.
- [x] DF-01: pass the actual image zoom button as optional second callback argument. Store it in a ref; on lightbox mount focus close button, and on cleanup restore connected opener. While lightbox is open capture Escape to close and Tab/Shift+Tab to retain focus on its only interactive element, including after an image click removes focus. Keep current backdrop click and frame stopPropagation. Related overlays: copy menu gets initial menuitem focus, Escape/outside click close and safe focus return; focused search results get Escape return to input and outside close without clearing query; settings Escape restores its trigger. Do not rewrite first-batch navigation logic or other provider components.
- [x] TB-15: turn the existing status live region into a visible fixed feedback pill using existing toolbar-refresh-status palette and local workbench-status positioning/wrapping. Retain role=status, aria-live=polite, and add aria-atomic. Reuse the same status state; no new persistence or onSaveLayout calls. Keep buttons/touch access.
- [x] Tests: >120 loaded/public-index results; load 120→240→final; no duplicates and later-result navigation; changing draft/filter/scope resets; lightbox Enter-open, internal click, Tab/Shift+Tab, Escape, close button/backdrop and opener focus; visible live region, copy success/failure and explicit-only save callback. Run affected tests with --silent to avoid old controller output overflow.
- [x] Verification: tsc; Workbench/App/ReaderView tests; optional isolated build output only if integration risk requires it; complete three-file incremental diff; independent read-only review; current 1420 source identity + Codex in-app browser >120/load/reset/lightbox/focus/feedback and fresh error logs. Wait for sync line service, do not restart it. Acceptance only after UI validation.

Commands from /Users/simon/OHB/one-holy-bible: `./node_modules/.bin/vitest run src/components/Workbench.test.tsx src/App.test.tsx src/components/ReaderView.test.tsx --reporter=dot --silent`; `./node_modules/.bin/tsc --noEmit`. Unrelated changes from the parallel synchronization task must be reported, not reverted.

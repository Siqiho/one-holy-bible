# Codex 当前页面抽查证据

日期：2026-09-09。执行者：Codex。浏览器：Codex 内置浏览器；URL 为 http://127.0.0.1:1420/，标题 One Holy Bible。本次为现状审计，未修改产品代码、未启动/重启服务、未执行保存/重置/删除/退回/同步操作。

## 运行面身份

PID 82698，2026-09-02 10:42:22 启动，命令 node /Users/simon/OHB/one-holy-bible/node_modules/.bin/vite --host 127.0.0.1 --port 1420 --strictPort，进程 cwd 为 /Users/simon/OHB/one-holy-bible。

本次新打开页面后，读取 Vite 返回的内联 sourcemap，sourcesContent 与以下磁盘源文件完全一致，排除本次审阅页面使用其他目录旧代码：

| 文件 | SHA-256 | served sourcemap matches disk |
|---|---|---|
| src/components/Workbench.tsx | ecea8115e47e07096107dfd063710e50b194111b302a2876a4974034e5a1180c | true |
| src/App.tsx | a4e54b85984fe35110950de3ec516d51249ca9f6a98618d98f275d8f9f201c0d | true |
| src/components/ReaderView.tsx | 39cb6d7cb312a7cd6f15496538dfde993b91918c4b14b46a4281c7ce112e049a | true |

## 观察结果

1. 初次进入为 Reader，创世记 1，英文对照开启，可见“已自动保存”和“阅读台”入口。点击“阅读台”进入原 Workbench。
2. 桌面 1600×1000 视口下确认主栏存在：书卷、章节、和合本/KJV/卡片模块开关、交换中间模块顺序、卡片搜索、经文搜索、阅读、刷新卡片、保存布局、重置。左栏有来源选择、已禁用的添加资源库、收起左侧资料栏和设置。两个搜索的存在并不证明功能冗余。
3. 该已保存布局中，和合本、卡片模块开启，KJV 关闭。这只是当前用户布局状态，不据此推断新用户默认状态。
4. 非选中第 2 节按钮 class=verse-button，实际计算 opacity=0.72。属于当前可见表现，不是历史推断。
5. 设置弹层当前只显示“纸张模式”和“书体”，两个开关均为 pressed=true。按 Esc 后 dialog 数量为 0；未改动开关。
6. 书卷选择含 66 卷。按 Esc 后书卷 dialog 数量仍为 1、aria-expanded=true；桌面宽度重查结果相同。点击标题区域外部后 dialog 数量为 0。再次点同一书卷触发器也曾观察到仍为打开状态，未单独定位事件原因，不能据此认定另一项已证实代码缺陷。
7. 卡片搜索输入 ctb审计无匹配20260909 后显示“0 张”；页面出现 4 处“当前经节还没有资源。”，而中间“当前经文卡片列表”仍有 34 个 article。该观察支持空态文案误导和过滤作用域需要解释；实际过滤范围以调用链核对为准。清除卡片搜索入口确实存在，并在桌面视口实际清除成功，不能沿用旧评审“没有清除入口”的说法。
8. 经文搜索输入“光”并按 Enter，结果为 429 处；实际结果按钮 120 个，另有 7 个译本/范围筛选按钮；末尾仅提示“已显示前 120 处结果。”，该面板未见加载更多按钮。
9. 将已提交的搜索词“光”改为“起初”但不再次提交，标题显示“点击搜索更新结果”，正文显示“输入已改变，点击搜索更新结果。”。完整后一句精确匹配只出现 1 次；存在同义提示重复，不能写成同一完整句子重复两次。
10. role=status 中业务反馈元素 class=sr-only，宽高各 1px、position=absolute；实际读到“搜索到 429 处经文”和后续“书卷选择已打开”。没有点击保存按钮，因此保存效果依据源码审阅，不声称本次实测保存。
11. 本次页面可读取的 warn/error 日志返回空数组，只覆盖本次捕获的浏览器日志，不代表全部历史或后台日志无错误。

## 收尾和限制

- 初始浏览器栏宽约 678px，工具栏有裁切。部分离屏按钮尝试没有改变页面状态，未将这些尝试记为应用功能失败；改为 1600×1000 桌面宽度后检查关键操作及状态。
- 最后确认两套搜索输入均为空，经文搜索面板已关闭；书卷弹层通过外点关闭；返回 Reader 并读到“阅读台”入口，视口 override 已 reset。
- 页面截图在本任务工具记录中可见；本文件是结构化现场观察，不冒充独立 Cursor 会话亲自进行的浏览器验证。
- 没有检查真实桌面 Tauri App，没有执行破坏性/持久数据路径，没有重跑全套测试或构建；本轮产物仅审计文档。
